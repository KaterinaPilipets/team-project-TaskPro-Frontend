import * as React from 'react';
import { Container, Button, Icon } from './Logout.styled';

const Logout = () => {
  return (
    <Container>
      <Button>
        <Icon />
        Log out
      </Button>
    </Container>
  );
};

export default Logout;
