import styled from '@emotion/styled';

export const Text = styled.p`
  font-size: 12px;
  line-height: 1.5;
  letter-spacing: -0.02em;
  padding-top: 8px;
  padding-top: 8px;
  color: var(--inputText);
  margin-bottom: 8px;
`;
